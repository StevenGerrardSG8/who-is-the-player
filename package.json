{
  "name": "who-is-the-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:data": "node scripts/sync-player-database.mjs",
    "validate:data": "node scripts/validate-player-database.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
