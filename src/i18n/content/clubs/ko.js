// Club-name translations, keyed by the exact club-name string as it appears
// in career arrays across the data files (English for most leagues, Hebrew
// for israeli-league.js). Missing keys gracefully fall back to the original
// text, so partial coverage is safe to ship incrementally.
export default {
};
