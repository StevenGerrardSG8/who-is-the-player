// Per-player localized answers, keyed by the exact "wiki" title (the stable
// unique id already used for photo lookup). Only add an entry when you have
// a real, natural localized/transliterated spelling for that player — a
// missing key gracefully falls back to the player's original pack-language
// answer, so partial coverage is always safe.
//   "<wiki title>": { answer: "<localized guessable spelling>" },
export default {
};
